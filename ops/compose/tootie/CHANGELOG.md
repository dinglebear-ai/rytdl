# Changelog

## Unreleased

- Added the product-owned TOOTIE Compose declaration for the persistent stdio runtime.
- Updated the image from the retired `ghcr.io/jmagar/ytdl-mcp:main` identity to `ghcr.io/dinglebear-ai/rytdl:main`.
- Added a path-independent `docker exec -i` MCP launcher and retained external `jakenet`, state, cache, and music-library mounts.
